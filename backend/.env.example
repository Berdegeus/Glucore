DATABASE_URL="postgresql://YOUR_USER@localhost:5432/glucore_dev?schema=public"
JWT_SECRET="change-me-before-production"
PORT=3001

# SMTP — leave blank to skip email sending (token printed to console in dev)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

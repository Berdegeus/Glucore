# Copy to .env.test and point at a throwaway database whose name contains "_test".
# The suite TRUNCATEs every table between cases.
TEST_DATABASE_URL="postgresql://USER@localhost:5432/glucore_test?schema=public"

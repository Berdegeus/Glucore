# Auto detect text files and perform LF normalization
* text=auto

# Gradle wrapper: `* text=auto` above would rewrite gradlew.bat to LF, which
# breaks it on Windows, and would corrupt the wrapper jar as if it were text.
android/gradlew        text eol=lf
android/gradlew.bat    text eol=crlf
*.jar                  binary
